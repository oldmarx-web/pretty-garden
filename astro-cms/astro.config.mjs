import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 배포 후 사이트 주소로 바꿔주세요 (예: 'https://prettygarden.kr')
  site: 'https://prettygarden.netlify.app',
});
